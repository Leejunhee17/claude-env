# ── Claude Code 런타임 상태 (로컬 전용, 커밋 금지) ──────────────────────────

# 세션/프로젝트/태스크 런타임 데이터
claude/sessions/
claude/projects/
claude/tasks/

# 개인 메모리 (로컬 AI 메모리)
claude/memory/

# 캐시 및 임시 파일
claude/cache/
claude/backups/
claude/history.jsonl
claude/file-history/
claude/session-env/
claude/shell-snapshots/
claude/telemetry/
claude/ide/

# 플랜 및 런타임 상태
claude/plans/
claude/.omc/
claude/.omc-config.json
claude/.omc-version.json

# CLAUDE.md 백업 파일
claude/CLAUDE.md.backup.*

# ── 인증 / 자격증명 (절대 커밋 금지) ─────────────────────────────────────────

claude/.claude.json
claude/.credentials.json

# ── 플러그인 런타임 파일 ─────────────────────────────────────────────────────
# settings.json의 enabledPlugins + extraKnownMarketplaces로 첫 실행 시 자동 설치됨.
# 캐시/설치 기록은 컨테이너 내부 절대경로를 포함하므로 커밋 금지.

claude/plugins/

# MCP 인증 캐시
claude/mcp-needs-auth-cache.json

# ── OMC 런타임 상태 ──────────────────────────────────────────────────────────
.omc/

# ── 시스템 파일 ──────────────────────────────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride
